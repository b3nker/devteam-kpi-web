import { Retrospective } from './retrospective';

describe('Retrospective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new Retrospective()).toBeTruthy();
  });
});
