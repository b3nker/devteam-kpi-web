import { StoryPointAdapter } from './story-point-adapter';

describe('StoryPointAdapter', () => {
  it('should create an instance', () => {
    expect(new StoryPointAdapter()).toBeTruthy();
  });
});
