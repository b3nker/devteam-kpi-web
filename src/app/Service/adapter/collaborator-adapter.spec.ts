import { CollaboratorAdapter } from './collaborator-adapter';

describe('CollaboratorAdapter', () => {
  it('should create an instance', () => {
    expect(new CollaboratorAdapter()).toBeTruthy();
  });
});
