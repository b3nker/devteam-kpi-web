import { SprintCommitmentAdapter } from './sprint-commitment-adapter';

describe('SprintCommitmentAdapter', () => {
  it('should create an instance', () => {
    expect(new SprintCommitmentAdapter()).toBeTruthy();
  });
});
